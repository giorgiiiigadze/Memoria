// React Native injects __DEV__ at runtime. Normally typed by @types/react-native
// (pulled in via expo/tsconfig.base), but declared here for environments where
// node_modules are absent.
declare const __DEV__: boolean
